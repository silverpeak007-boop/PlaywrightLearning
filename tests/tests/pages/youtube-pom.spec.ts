/*

import { test } from '@playwright/test';
import { YoutubePage } from './Youtubepage';

test('youtube pom demo', async ({ page }) => {

    const youtubePage = new YoutubePage(page);

    await youtubePage.openYoutube();

});

*/